#pragma once

// Generated by CMake from util/version.hpp.in — the single source of truth for
// the version is project(tuix VERSION ...) in CMakeLists.txt. The release
// workflow asserts that the pushed tag matches what this expands to, so a
// mistagged release fails before any artifact is built.
#define TUIX_VERSION "@PROJECT_VERSION@"
